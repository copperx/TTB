@model TTB_Model.eventType

@{
    Layout = "~/Views/Shared/_Layout_Main.cshtml";
}

<h2>Event Types</h2>

@using (Html.BeginForm("AddEditEventType", "Admin"))
{
    <div class="container">
        <div class="col-md-8">
            <div class="form-group">
                <label for="name">Name</label>
                @Html.TextBoxFor(m => m.eventTypeName, new { @class = "form-control" })
            </div>
            @Html.HiddenFor(e => e.eventTypeID)
        </div>
        <div class="col-md-8">
            <input type="submit" class="btn btn-success btn-lg btnSaveEdit" value="Save" />
        </div>
    </div>
}
